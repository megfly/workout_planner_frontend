import React from 'react'

export default function ExerciseDisplay(props) {
    //debugger 
    return (
        <div>
            {props.name} 
            {/* - {props.sets} - {props.reps} - {props.weight} */}
        </div>
    )
}
